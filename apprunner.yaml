# Generated by Copilot
version: 1.0
runtime: python311

build:
  commands:
    build:
      - pip3 install -r requirements.txt

run:
  runtime-version: 3.11
  pre-run:
    - pip3 install -r requirements.txt
  command: uvicorn main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
  env:
    - name: PORT
      value: "8080"
    - name: S3_BUCKET_NAME
      value: "ept-data"
    - name: USE_S3
      value: "true"
    - name: FRONTEND_URL
      value: "https://bynixti6xn.us-east-1.awsapprunner.com"
